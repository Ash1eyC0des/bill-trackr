<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Bill Trackr</title>
    <link rel="stylesheet" href="css/style.css">
</head>
<body>
    <h1>Bills</h1>
    <ul>
    <% bills.forEach( el => { %>
            <li class='billItem' data-id='<%=el._id%>'>
                <span class='<%= el.completed === true ? 'completed' : 'not'%>'><%= el.bill %></span>
                <span class='del'> Delete </span>
            </li>
    <% }) %>    
    </ul>

    <h2><%= user.userName %> has <%= left %> bills left to pay.</h2>

    <form action="/bills/createBill" method='POST'>
        <input type="text" placeholder="Enter Bill" name='billItem'>
        <input type="submit">
    </form>

    <a href="/logout">Logout</a>

    <script src="/js/main.js"></script>
</body>
</html>